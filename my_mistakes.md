1. I got the `wp-config.php` in rce from other componenet than wordpress by instinct was if the wordpress is being run by another user i would escalate to that after logging as admin and getting rce from wordpress it was run as same `www-data` and i started to looking for other ways to get in and gave up too early instead one of the users from `/etc/passwd` simply had it's ssh same password same as the one i found in `wp-config.php`
